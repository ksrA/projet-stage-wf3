<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <title>Titre de la page</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta.3/css/bootstrap.min.css" integrity="sha384-Zug+QiDoJOrZ5t4lssLdxGhVrurbmBWopoEl+M6BdEfwnCJZtKxi1KgxUyJq13dy" crossorigin="anonymous">
    <link rel="stylesheet" href="/assets/css/param-compte/change-forgot-password.css">
</head>
<body>
    <div class="container-fluid mt-4">
        <div class="row justify-content-center">
            <div class="col-12 col-md-6">
                 {% if change is not defined %}
                    <h1>Définition nouveau mot de passe</h1>


                    {% if notchange is defined %}
                        {% form_theme formChange 'bootstrap_4_layout.html.twig' %}

                        {{ form(formChange) }}

                        <p>Les mots de passe doivent être identiques</p>
                    {% else %}
                        {% form_theme formChange 'bootstrap_4_layout.html.twig' %}

                        {{ form(formChange) }}
                    {% endif %}
                {% else %}
                    <div class="alert alert-success" role="alert">
                        Mot de passe changé !
                        <a href="{{ path('login') }}">Se connecter</a>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
</body>
</html>